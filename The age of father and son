def age_difference(f_age, s_age):
    if f_age == 2 * s_age:
        return 0
    elif f_age > 2 * s_age:
        return f_age - 2 * s_age
    else:
        return 2 * s_age - f_age
print(age_difference(36, 7))
print(age_difference(55, 30))
print(age_difference(40, 20))
